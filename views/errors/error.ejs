<%# implement a value for the <title> element thats closely associated to have a <h1> heading that does the same thing. %>
<% if (title) { %>
    <h1><%= title %></h1>
    <% } else {
        res.redirect('/')
    } %>

<%- messages() %>